---
// Import SecondaryCTA component for use in this module
import SecondaryCTA from "@components/ui/buttons/SecondaryCTA.astro";
import Icon from "@components/ui/icons/Icon.astro";

// Define props from Astro
const { pricing } = Astro.props;
---



<section
  class="mx-auto max-w-[85rem] px-4 py-30 sm:px-6 lg:px-8 2xl:max-w-full"
>
  <!-- Section heading and sub-heading -->
  <div class="mx-auto mb-10 max-w-2xl text-center lg:mb-14">
    <h2
      class="text-balance text-2xl tracking-tight text-neutral-800 dark:text-neutral-200 md:text-4xl md:leading-tight"
    >
      {pricing.title}
    </h2>
    <p class="mt-1 text-pretty text-neutral-600 dark:text-neutral-400">
      {pricing.subTitle}
    </p>
  </div>
  <!-- Contains the work-together plan cards -->
  <div class="grid gap-4 sm:grid-cols-3 sm:gap-6">
    {
      pricing.plans.map((plan: { name: string; description: string; features: string[] }, index: number) => (
        <div
          class={`flex w-full flex-col rounded-xl p-6 sm:p-8 ${
            index === 1
              ? "bg-gradient-to-tr from-[#2A3A8F] to-[#2198F9] shadow-xl"
              : "bg-gray-800"
          }`}
        >
          <div class="mb-4">
            <h3 class="text-2xl text-neutral-100">{plan.name}</h3>
            <p class="text-indigo-300">{plan.description}</p>
          </div>

          <ul class="space-y-2 text-neutral-100">
            {plan.features.map((feature: string) => (
              <li class="flex items-start gap-1.5">
                <Icon name="checkCircle" />
                <span>{feature}</span>
              </li>
            ))}
          </ul>
        </div>
      ))
    }
  </div>
  <!-- Call to action for Enterprise Solutions -->
  <div class="mt-8 flex items-center justify-center gap-x-3 md:mt-12">
    <p class="text-sm text-neutral-600 dark:text-neutral-400">
      {pricing.thirdOption}
    </p>

    <SecondaryCTA title={pricing.btnText} url="/contact" />
  </div>
</section>
